name: Build Flutter APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Generate Platform Folders & Build APK
        working-directory: ./flutter_app
        run: |
          flutter config --enable-android
          flutter create --platforms=android .
          flutter pub get
          dart run flutter_launcher_icons || echo "flutter_launcher_icons failed"
          echo "Before sed:"
          grep "android:label" android/app/src/main/AndroidManifest.xml || true
          sed -i 's/android:label="[^"]*"/android:label="First team"/g' android/app/src/main/AndroidManifest.xml
          sed -i '/<application/a \        android:usesCleartextTraffic="true"' android/app/src/main/AndroidManifest.xml
          sed -i '/<manifest/a \    <uses-permission android:name="android.permission.INTERNET" />' android/app/src/main/AndroidManifest.xml
          echo "After sed:"
          grep "android:label" android/app/src/main/AndroidManifest.xml || true
          flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Robotic-Controller-APK
          path: flutter_app/build/app/outputs/flutter-apk/app-release.apk
